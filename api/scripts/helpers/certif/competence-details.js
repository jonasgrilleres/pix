// eslint-disable-next-line no-unused-vars
function showDetails(elementToShowDetails) {
  elementToShowDetails.className = elementToShowDetails.className === 'details--visible'
    ? 'details--invisible'
    : 'details--visible';
}
